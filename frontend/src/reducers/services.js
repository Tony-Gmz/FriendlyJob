const initialState = {
  // Initial State
  /** List of service */
  serviceList: [],
};

const servicesReducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default: return state;
  }
};

export default servicesReducer;
